#!/bin/bash
# This script is generated by Labtasker from experiment_guidance_scale.sh

source <(envidia --cuda $1)

for cfg in 7.5 15 30 60 120; do
  for algorithm in sds vsd ism; do
    labtasker task submit -- --algorithm "$algorithm" --cfg "$cfg"
  done
done
